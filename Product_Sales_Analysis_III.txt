SELECT 
    s.product_id, 
    s.year AS first_year, 
    s.quantity, 
    s.price
FROM 
    Sales s
JOIN 
    Product p
ON 
    s.product_id = p.product_id
WHERE 
    s.year = (
        SELECT MIN(year)
        FROM Sales s2
        WHERE s2.product_id = s.product_id
    );
