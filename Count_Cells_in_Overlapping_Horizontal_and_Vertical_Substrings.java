class Solution {
    public int countCells(char[][] grid, String pattern) {
        int m=grid.length;
        int n=grid[0].length;
        if (pattern.length()==1 && m*n==1){
            return 1;
        }
        Set<Set<String>> set1=new HashSet<>();
        Set<Set<String>> set2=new HashSet<>();
        
        return 1;
    }
}